# 学习文章自动更新

这个版本保持原有网页样式。GitHub Pages 发布前，程序扫描 `zeksa` 目录里的文章，读取标题和简介，生成主页的文章入口。

## 首次设置

本配置将仓库根目录作为网站发布根目录，学习主页和文章统一放在其下的 `zeksa` 目录中。

1. 将压缩包中的 `zeksa` 目录合并到网站仓库，保留你已有的其他网页与资源。
2. 将 `.github/workflows/zeksa-pages.yml` 放到仓库的同名路径。`.github` 是隐藏目录；也可以在 GitHub 的 Add file → Create new file 中输入这个完整路径，再粘贴配置内容。
3. 在仓库 Settings → Pages → Build and deployment 中，将 Source 设为 **GitHub Actions**。
4. 将文件提交到仓库的默认分支。打开 Actions，等待“更新文章并发布网站”完成。如果改设置前任务已失败，可在该任务页面重新运行。

当前保留你的主页文件名 `tindex.html`，学习主页地址为 `/zeksa/tindex.html`。

如果仓库已经有自己的 GitHub Actions 发布流程，不要同时启用两套部署：只把 `python3 zeksa/update_articles.py --home tindex.html` 加到现有流程的“打包网站”之前。

如果网站目前发布自 `docs` 目录或依赖 Jekyll 等构建步骤，需要按现有发布结构调整流程；不要直接用这份根目录纯 HTML 配置替换它。

## 以后添加文章

1. 复制 `article-template.html`，另存为同目录的新文件，例如 `article-01.html`。
2. 填写文章头部的标题和简介：

```html
<header class="page-header">
  <h1>你的文章标题</h1>
  <p class="intro">你的文章简介</p>
</header>
```

3. 填写正文，保留正文容器的 `article-body` 类。
4. 将新文章提交到 GitHub 默认分支。部署成功后刷新主页，就会出现新条目。

无需手动填写主页链接、文件名清单、标题或简介。修改文章标题、简介以及删除文章，也会在下一次发布时同步。

## 读取规则

- 扫描与主页同目录的 `.html` 文件，不扫描子目录。
- 只收录包含 `<article class="... article-body ...">` 正文容器的页面。
- 标题读取第一个可见的 `<h1>`。
- 简介读取 `.page-header` 中的第一个 `.intro`；没有时读取 `<meta name="description" content="…">`。两者都没有就不显示简介，不自动编造摘要。
- 模板文件、空标题、标题仍为 `【文章标题】` 的文件不会进入列表。
- 可以在文章 `<head>` 加 `<meta name="zeksa-draft" content="true">` 暂时不列入目录。这仅控制列表显示，不会阻止该文件随网站发布。
- 条目按文件名排序，建议使用 `article-01.html`、`article-02.html` 等名称控制顺序。
- 只改写主页 `ARTICLES:START` 与 `ARTICLES:END` 之间的列表，其余内容保留。

列表在发布时生成，不会回写到 GitHub 仓库；访客访问到的是已生成的页面。网页不需要调用 GitHub API，也不需要浏览器逐篇下载文章。

## 在电脑上预览

GitHub 的自动更新发生在发布时。如果想在电脑上同步，安装 Python 3 后运行：

```sh
python3 update_articles.py
```

也可以保持自动监测：

```sh
python3 update_articles.py --watch
```

运行监测时，保存、新增或删除文章后会自动更新本地主页；浏览器刷新后可见。按 Ctrl+C 退出。

如果以后把主页改名为 `index.html`，同时修改模板内的返回链接、发布配置中的 `--home` 参数，并使用 `python3 update_articles.py --home index.html`。

GitHub Pages 工作流参考：https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages
